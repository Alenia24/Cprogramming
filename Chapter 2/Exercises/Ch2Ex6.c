/*
  Section 2.7
  Why is it not a good idea for an identifier to contain more
  than one adjacent underscore (as in current__balance, for example)?

  Answer:
  Identifiers that contain more than one adjacent underscore may be
  typed incorrectly which can lead to errors.
*/
